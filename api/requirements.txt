# Pin Scrapling so upstream pulls into the fork never silently change the API's behaviour.
# Bump this deliberately (and re-test) to adopt a newer Scrapling.
# [rag] = [fetchers] + markdownify (HTML -> Markdown, imported lazily by
# scrapling.core.shell.Convertor). Since 0.4.15 markdownify lives behind this extra.
scrapling[rag]==0.4.15

fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic-settings>=2.5,<3.0
