"""Scrapling Scrape API — a thin REST wrapper around the Scrapling library.

This package lives OUTSIDE the Scrapling source tree (it only *imports* scrapling
as a pinned dependency), so the fork can keep pulling from upstream cleanly.
"""

__version__ = "1.0.0"
